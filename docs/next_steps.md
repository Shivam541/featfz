# Next Steps

This document captures the main follow-up items left after the assignment delivery.

## Planned Follow-Up

- Add end-to-end tests that cover the UI and backend together.
- Improve the UI for managing flags.
- Remove the manual tenant secret addition dependency.
- Improve API performance with safe caching.

## Notes

- Keep tenant isolation as a hard requirement.
- Keep the admin UI modular instead of merging the workflows back into one screen.
- Update the docs whenever the API or frontend flow changes.

